# Dev-only. DB_USERNAME/DB_PASSWORD must match k8s/mysql/secret.yaml.
# JWT_SECRET below is a random placeholder generated for this local stack —
# it is NOT pulled from your real application-secrets.yaml (that file isn't
# in the zip you gave me, correctly, since it's gitignored). Rotate this
# before it signs anything that matters; any 32+ byte random string works.
apiVersion: v1
kind: Secret
metadata:
  name: echo-backend-secrets
  namespace: echo
type: Opaque
stringData:
  DB_USERNAME: echo
  DB_PASSWORD: echo
  JWT_SECRET: "dev-only-3f9a1c7e5b2d4681c0f7a9e2b6d4381f5e2a7c9b1d3f6082a4c7e9b2d5f8a1c3"
